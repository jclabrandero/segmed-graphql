
type Insured {
	id:			Int!
	code:		String!
	iin:		Int
	inletDate:	DateTime!
	outletDate:	DateTime
	tradeUnion:	Boolean!
	address:	String
	phone:		String

	status:			Int!

	person:			Person!
	insuredType:	InsuredType!

	holderInsured:	Insured
	dependents:		[Insured!]!
	belonging:		Belonging!
}


input IInsuredCreateArgs {
	iin:		Int
	inletDate:	DateTime
	tradeUnion:	Boolean
	address:	String
	phone:		String

	personId:			Int!
	insuredTypeId: 		Int!
	holderInsuredId:	Int
	belongingId:		Int!
}

input IInsuredUpdateArgs {
	iin:		Int
	inletDate:	DateTime
	outletDate:	DateTime
	tradeUnion:	Boolean
	address:	String
	phone:		String

	personId:			Int
	insuredTypeId: 		Int
	holderInsuredId:	Int
	belongingId:		Int
}

type Query {
	insureds: [Insured!]! @authorized @has(permissions: [ReadInsured])
	activeInsureds: [Insured!]! @authorized @has(permissions: [ReadInsured])
	activeHolderInsureds: [Insured!]! @authorized @has(permissions: [ReadInsured])
	insuredsWithClinicCares: [Insured!]! @authorized @has(permissions: [ReadInsured])
	insured(id: Int!): Insured! @authorized @has(permissions: [ReadInsured])
}

type Mutation {
	createInsured(data: IInsuredCreateArgs!): Insured! @authorized @has(permissions: [WriteInsured])
	updateInsured(id: Int!, data: IInsuredUpdateArgs!): Insured! @authorized @has(permissions: [WriteInsured])
	deleteInsured(id: Int!): Insured! @authorized @has(permissions: [WriteInsured])
	upgradeInsured(id: Int!): Insured! @authorized @has(permissions: [WriteInsured])
	downgradeInsured(id: Int!): Insured! @authorized @has(permissions: [WriteInsured])
}

type Subscription {
	insuredCreated: Insured!
	insuredUpdated: Insured!
	insuredDeleted: Insured!
	insuredUpserted: Insured!
}
