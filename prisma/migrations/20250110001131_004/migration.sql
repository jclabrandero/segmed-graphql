BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Inventory] ADD [price] DECIMAL(32,16) NOT NULL CONSTRAINT [Inventory_price_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
