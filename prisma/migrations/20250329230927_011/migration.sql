BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[DepartureClinicCare] DROP CONSTRAINT [DepartureClinicCare_clinicCareId_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
