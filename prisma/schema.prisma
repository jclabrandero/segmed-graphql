//
// Autogenerated by `prisma-import`
// Any modifications will be overwritten on subsequent runs.
//

//
// source.prisma
//

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE")
}

generator client {
  provider = "prisma-client-js"
}

model Audit {
  id              BigInt   @id @default(autoincrement())
  tableName       String
  fieldName       String
  rowId           Int
  oldValue        String?
  newValue        String?
  host            String
  application     String
  applicationUser String
  userName        String
  date            DateTime
}

//
// group.model.prisma
//

model Group {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  members     UserGroup[]
  permissions GroupPermission[]

  creatorUser    User  @relation(name: "GroupCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "GroupLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model GroupPermission {
  id Int @unique @default(autoincrement())

  groupId      Int
  permissionId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  group      Group      @relation(fields: [groupId], references: [id])
  permission Permission @relation(fields: [permissionId], references: [id])

  creatorUser    User  @relation(name: "GroupPermissionCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "GroupPermissionLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([groupId, permissionId])
}

//
// permission.model.prisma
//

model Permission {
  id          Int     @id @default(autoincrement())
  code        String  @unique
  name        String  @unique
  description String?

  status Int @default(1)

  groups GroupPermission[]
}

//
// session.model.prisma
//

model Session {
  id         Int    @id @default(autoincrement())
  publicKey  String @db.VarChar(Max)
  privateKey String @db.VarChar(Max)

  status Int @default(1)

  userName String

  user User @relation(fields: [userName], references: [userName])
}

//
// user.model.prisma
//

model User {
  id          Int     @id @default(autoincrement())
  userName    String  @unique
  displayName String?
  email       String?
  directory   String  @default("LOCAL") // LOCAL, AD,

  status Int @default(1)

  groups    UserGroup[]
  passwords UserPassword[]
  clerkUser ClerkUser?

  createdGroups           Group[]           @relation(name: "GroupCreatorUser")
  updatedGroups           Group[]           @relation(name: "GroupLastUpdateUser")
  createdGroupPermissions GroupPermission[] @relation(name: "GroupPermissionCreatorUser")
  updatedGroupPermissions GroupPermission[] @relation(name: "GroupPermissionLastUpdateUser")
  createdUserGroups       UserGroup[]       @relation(name: "UserGroupCreatorUser")
  updatedUserGroups       UserGroup[]       @relation(name: "UserGroupLastUpdateUser")
  createdFileUploads      FileUpload[]      @relation(name: "FileUploadCreatorUser")
  updatedFileUploads      FileUpload[]      @relation(name: "FileUploadLastUpdateUser")

  createdPersonDocumentTypes            PersonDocumentType[]           @relation(name: "PersonDocumentTypeCreatorUser")
  updatedPersonDocumentTypes            PersonDocumentType[]           @relation(name: "PersonDocumentTypeLastUpdateUser")
  createdEmployeeTypes                  EmployeeType[]                 @relation(name: "EmployeeTypeCreatorUser")
  updatedEmployeeTypes                  EmployeeType[]                 @relation(name: "EmployeeTypeLastUpdateUser")
  createdEmployeePositions              EmployeePosition[]             @relation(name: "EmployeePositionCreatorUser")
  updatedEmployeePositions              EmployeePosition[]             @relation(name: "EmployeePositionLastUpdateUser")
  createdInsuredTypes                   InsuredType[]                  @relation(name: "InsuredTypeCreatorUser")
  updatedInsuredTypes                   InsuredType[]                  @relation(name: "InsuredTypeLastUpdateUser")
  createdMedicalSubspecialties          MedicalSubspecialty[]          @relation(name: "MedicalSubspecialtyCreatorUser")
  updatedMedicalSubspecialties          MedicalSubspecialty[]          @relation(name: "MedicalSubspecialtyLastUpdateUser")
  createdMedicalSpecialties             MedicalSpecialty[]             @relation(name: "MedicalSpecialtyCreatorUser")
  updatedMedicalSpecialties             MedicalSpecialty[]             @relation(name: "MedicalSpecialtyLastUpdateUser")
  createdMedicalSpecialtySubspecialties MedicalSpecialtySubspecialty[] @relation(name: "MedicalSpecialtySubspecialtyCreatorUser")
  updatedMedicalSpecialtySubspecialties MedicalSpecialtySubspecialty[] @relation(name: "MedicalSpecialtySubspecialtyLastUpdateUser")
  createdMedicalGroups                  MedicalGroup[]                 @relation(name: "MedicalGroupCreatorUser")
  updatedMedicalGroupes                 MedicalGroup[]                 @relation(name: "MedicalGroupLastUpdateUser")
  createdMedicalGroupSpecialties        MedicalGroupSpecialty[]        @relation(name: "MedicalGroupSpecialtyCreatorUser")
  updatedMedicalGroupSpecialties        MedicalGroupSpecialty[]        @relation(name: "MedicalGroupSpecialtyLastUpdateUser")
  createdDrugClasess                    DrugClass[]                    @relation(name: "DrugClassCreatorUser")
  updatedDrugClasess                    DrugClass[]                    @relation(name: "DrugClassLastUpdateUser")
  createdDrugUnits                      DrugUnit[]                     @relation(name: "DrugUnitCreatorUser")
  updatedDrugUnits                      DrugUnit[]                     @relation(name: "DrugUnitLastUpdateUser")
  createdClinicalCareStates             ClinicalCareState[]            @relation(name: "ClinicalCareStateCreatorUser")
  updatedClinicalCareStates             ClinicalCareState[]            @relation(name: "ClinicalCareStateLastUpdateUser")
  createdDisabilityTypes                DisabilityType[]               @relation(name: "DisabilityTypeCreatorUser")
  updatedDisabilityTypes                DisabilityType[]               @relation(name: "DisabilityTypeLastUpdateUser")

  createdBelongings                    Belonging[]                   @relation(name: "BelongingCreatorUser")
  updatedBelongings                    Belonging[]                   @relation(name: "BelongingLastUpdateUser")
  createdMedicalOffices                MedicalOffice[]               @relation(name: "MedicalOfficeCreatorUser")
  updatedMedicalOffices                MedicalOffice[]               @relation(name: "MedicalOfficeLastUpdateUser")
  createdProviders                     Provider[]                    @relation(name: "ProviderCreatorUser")
  updatedProviders                     Provider[]                    @relation(name: "ProviderLastUpdateUser")
  createdProviderMedicalGroups         ProviderMedicalGroup[]        @relation(name: "ProviderMedicalGroupCreatorUser")
  updatedProviderMedicalGroups         ProviderMedicalGroup[]        @relation(name: "ProviderMedicalGroupLastUpdateUser")
  createdProviderMedicalSpecialties    ProviderMedicalSpecialty[]    @relation(name: "ProviderMedicalSpecialtyCreatorUser")
  updatedProviderMedicalSpecialties    ProviderMedicalSpecialty[]    @relation(name: "ProviderMedicalSpecialtyLastUpdateUser")
  createdProviderMedicalSubspecialties ProviderMedicalSubspecialty[] @relation(name: "ProviderMedicalSubspecialtyCreatorUser")
  updatedProviderMedicalSubspecialties ProviderMedicalSubspecialty[] @relation(name: "ProviderMedicalSubspecialtyLastUpdateUser")

  createdPersons             Person[]             @relation(name: "PersonCreatorUser")
  updatedPersons             Person[]             @relation(name: "PersonLastUpdateUser")
  createdClerks              Clerk[]              @relation(name: "ClerkCreatorUser")
  updatedClerks              Clerk[]              @relation(name: "ClerkLastUpdateUser")
  createdClerkUsers          ClerkUser[]          @relation(name: "ClerkUserCreatorUser")
  updatedClerkUsers          ClerkUser[]          @relation(name: "ClerkUserLastUpdateUser")
  createdClerkMedicalOffices ClerkMedicalOffice[] @relation(name: "ClerkMedicalOfficeCreatorUser")
  updatedClerkMedicalOffices ClerkMedicalOffice[] @relation(name: "ClerkMedicalOfficeLastUpdateUser")
  createdInsureds            Insured[]            @relation(name: "InsuredCreatorUser")
  updatedInsureds            Insured[]            @relation(name: "InsuredLastUpdateUser")

  createdMedications    Medication[]    @relation(name: "MedicationCreatorUser")
  updatedMedications    Medication[]    @relation(name: "MedicationLastUpdateUser")
  createdPharmacies     Pharmacy[]      @relation(name: "PharmacyCreatorUser")
  updatedPharmacies     Pharmacy[]      @relation(name: "PharmacyLastUpdateUser")
  createdBatches        Batch[]         @relation(name: "BatchCreatorUser")
  updatedBatches        Batch[]         @relation(name: "BatchLastUpdateUser")
  createdInventories    Inventory[]     @relation(name: "InventoryCreatorUser")
  updatedInventories    Inventory[]     @relation(name: "InventoryLastUpdateUser")
  createdArrivals       Arrival[]       @relation(name: "ArrivalCreatorUser")
  updatedArrivals       Arrival[]       @relation(name: "ArrivalLastUpdateUser")
  createdArrivalItems   ArrivalItem[]   @relation(name: "ArrivalItemCreatorUser")
  updatedArrivalItems   ArrivalItem[]   @relation(name: "ArrivalItemLastUpdateUser")
  createdDepartures     Departure[]     @relation(name: "DepartureCreatorUser")
  updatedDepartures     Departure[]     @relation(name: "DepartureLastUpdateUser")
  createdDepartureItems DepartureItem[] @relation(name: "DepartureItemCreatorUser")
  updatedDepartureItems DepartureItem[] @relation(name: "DepartureItemLastUpdateUser")

  createdClinicCares                        ClinicCare[]                       @relation(name: "ClinicCareCreatorUser")
  updatedClinicCares                        ClinicCare[]                       @relation(name: "ClinicCareLastUpdateUser")
  createdClinicCarePrimaries                ClinicCarePrimary[]                @relation(name: "ClinicCarePrimaryCreatorUser")
  updatedClinicCarePrimaries                ClinicCarePrimary[]                @relation(name: "ClinicCarePrimaryLastUpdateUser")
  createdClinicCareInsureds                 ClinicCareInsured[]                @relation(name: "ClinicCareInsuredCreatorUser")
  updatedClinicCareInsureds                 ClinicCareInsured[]                @relation(name: "ClinicCareInsuredLastUpdateUser")
  createdClinicCareMedicalOffices           ClinicCareMedicalOffice[]          @relation(name: "ClinicCareMedicalOfficeCreatorUser")
  updatedClinicCareMedicalOffices           ClinicCareMedicalOffice[]          @relation(name: "ClinicCareMedicalOfficeLastUpdateUser")
  createdInterclinicals                     Interclinical[]                    @relation(name: "InterclinicalCreatorUser")
  updatedInterclinicals                     Interclinical[]                    @relation(name: "InterclinicalLastUpdateUser")
  createdInterclinicalProvider              InterclinicalProvider[]            @relation(name: "InterclinicalProviderCreatorUser")
  updatedInterclinicalProvider              InterclinicalProvider[]            @relation(name: "InterclinicalProviderLastUpdateUser")
  createdInterclinicalMedicalGroup          InterclinicalMedicalGroup[]        @relation(name: "InterclinicalMedicalGroupCreatorUser")
  updatedInterclinicalMedicalGroup          InterclinicalMedicalGroup[]        @relation(name: "InterclinicalMedicalGroupLastUpdateUser")
  createdInterclinicalMedicalSpecialties    InterclinicalMedicalSpecialty[]    @relation(name: "InterclinicalMedicalSpecialtyCreatorUser")
  updatedInterclinicalMedicalSpecialties    InterclinicalMedicalSpecialty[]    @relation(name: "InterclinicalMedicalSpecialtyLastUpdateUser")
  createdInterclinicalMedicalSubspecialties InterclinicalMedicalSubspecialty[] @relation(name: "InterclinicalMedicalSubspecialtyCreatorUser")
  updatedInterclinicalMedicalSubspecialties InterclinicalMedicalSubspecialty[] @relation(name: "InterclinicalMedicalSubspecialtyLastUpdateUser")
  createdInterclinicalFileUploads           InterclinicalFileUpload[]          @relation(name: "InterclinicalFileUploadCreatorUser")
  updatedInterclinicalFileUploads           InterclinicalFileUpload[]          @relation(name: "InterclinicalFileUploadLastUpdateUser")
  createdPrescriptions                      Prescription[]                     @relation(name: "PrescriptionCreatorUser")
  updatedPrescriptions                      Prescription[]                     @relation(name: "PrescriptionLastUpdateUser")
  createdPrescriptionPharmacies             PrescriptionPharmacy[]             @relation(name: "PrescriptionPharmacyCreatorUser")
  updatedPrescriptionPharmacies             PrescriptionPharmacy[]             @relation(name: "PrescriptionPharmacyLastUpdateUser")
  createdPrescriptionMedications            PrescriptionMedication[]           @relation(name: "PrescriptionMedicationCreatorUser")
  updatedPrescriptionMedications            PrescriptionMedication[]           @relation(name: "PrescriptionMedicationLastUpdateUser")
  createdPrescriptionExterns                PrescriptionExtern[]               @relation(name: "PrescriptionExternCreatorUser")
  updatedPrescriptionExterns                PrescriptionExtern[]               @relation(name: "PrescriptionExternLastUpdateUser")
  createdPrescriptionExternMedications      PrescriptionExternMedication[]     @relation(name: "PrescriptionExternMedicationCreatorUser")
  updatedPrescriptionExternMedications      PrescriptionExternMedication[]     @relation(name: "PrescriptionExternMedicationLastUpdateUser")
  approvedMedicalLeaves                     MedicalLeave[]                     @relation(name: "MedicalLeaveApprovalUser")
  createdMedicalLeaves                      MedicalLeave[]                     @relation(name: "MedicalLeaveCreatorUser")
  updatedMedicalLeaves                      MedicalLeave[]                     @relation(name: "MedicalLeaveLastUpdateUser")
  createdMedicalLeaveDisabilityTypes        MedicalLeaveDisabilityType[]       @relation(name: "MedicalLeaveDisabilityTypeCreatorUser")
  updatedMedicalLeaveDisabilityTypes        MedicalLeaveDisabilityType[]       @relation(name: "MedicalLeaveDisabilityTypeLastUpdateUser")
  Session                                   Session[]
}

model UserGroup {
  id Int @unique @default(autoincrement())

  userId  Int
  groupId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  user  User  @relation(fields: [userId], references: [id])
  group Group @relation(fields: [groupId], references: [id])

  creatorUser    User  @relation(name: "UserGroupCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "UserGroupLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([userId, groupId])
}

model UserPassword {
  id        Int    @unique @default(autoincrement())
  encrypted String
  status    Int    @default(1)

  userName String

  user User @relation(fields: [userName], references: [userName])

  @@id([userName, encrypted])
}

//
// clinical-care-state.model.prisma
//

model ClinicalCareState {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?
  color       String
  lock        Boolean

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  clinicCareRecords ClinicCare[]

  creatorUser    User  @relation(name: "ClinicalCareStateCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClinicalCareStateLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// disability-type.prisma
//

model DisabilityType {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalLeaves MedicalLeaveDisabilityType[]

  creatorUser    User  @relation(name: "DisabilityTypeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "DisabilityTypeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// drug-class.model.prisma
//

model DrugClass {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medications Medication[]

  creatorUser    User  @relation(name: "DrugClassCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "DrugClassLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// drug-unit.model.prisma
//

model DrugUnit {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medications Medication[]

  creatorUser    User  @relation(name: "DrugUnitCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "DrugUnitLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// employee-position.model.prisma
//

model EmployeePosition {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  employees Clerk[]

  creatorUser    User  @relation(name: "EmployeePositionCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "EmployeePositionLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// employee-type.model.prisma
//

model EmployeeType {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  employees Clerk[]

  creatorUser    User  @relation(name: "EmployeeTypeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "EmployeeTypeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// insured-type.model.prisma
//

model InsuredType {
  id             Int     @id @default(autoincrement())
  name           String  @unique
  description    String?
  withDependents Boolean
  codeFormat     String  @default("AA-MMDD-XXX")
  outletAge      Int?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  insureds Insured[]

  creatorUser    User  @relation(name: "InsuredTypeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InsuredTypeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// medical-group.model.prisma
//

model MedicalGroup {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  specialties MedicalGroupSpecialty[]
  providers   ProviderMedicalGroup[]

  creatorUser               User                        @relation(name: "MedicalGroupCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser            User?                       @relation(name: "MedicalGroupLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  InterclinicalMedicalGroup InterclinicalMedicalGroup[]
}

model MedicalGroupSpecialty {
  id Int @unique @default(autoincrement())

  medicalSpecialtyId Int
  medicalGroupId     Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalSpecialty MedicalSpecialty @relation(fields: [medicalSpecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  medicalGroup     MedicalGroup     @relation(fields: [medicalGroupId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser    User  @relation(name: "MedicalGroupSpecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "MedicalGroupSpecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalSpecialtyId, medicalGroupId])
}

//
// medical-specialty.model.prisma
//

model MedicalSpecialty {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  subspecialties      MedicalSpecialtySubspecialty[]
  groups              MedicalGroupSpecialty[]
  providerSpecialties ProviderMedicalSpecialty[]

  creatorUser                   User                            @relation(name: "MedicalSpecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser                User?                           @relation(name: "MedicalSpecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  InterclinicalMedicalSpecialty InterclinicalMedicalSpecialty[]
}

model MedicalSpecialtySubspecialty {
  id Int @unique @default(autoincrement())

  medicalSubspecialtyId Int
  medicalSpecialtyId    Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalSubspecialty MedicalSubspecialty @relation(fields: [medicalSubspecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  medicalSpecialty    MedicalSpecialty    @relation(fields: [medicalSpecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser    User  @relation(name: "MedicalSpecialtySubspecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "MedicalSpecialtySubspecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalSubspecialtyId, medicalSpecialtyId])
}

//
// medical-subspecialty.model.prisma
//

model MedicalSubspecialty {
  id   Int    @id @default(autoincrement())
  name String @unique

  description      String?
  ageRangePatients String?

  dt String?
  si String?
  ot String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  subspecialtyOf MedicalSpecialtySubspecialty[]

  creatorUser                      User                               @relation(name: "MedicalSubspecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser                   User?                              @relation(name: "MedicalSubspecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  InterclinicalMedicalSubspecialty InterclinicalMedicalSubspecialty[]
  ProviderMedicalSubspecialty      ProviderMedicalSubspecialty[]
}

//
// person-document-type.model.prisma
//

model PersonDocumentType {
  id          Int     @id @default(autoincrement())
  name        String  @unique
  description String?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  people Person[]

  creatorUser    User  @relation(name: "PersonDocumentTypeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "PersonDocumentTypeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// batch.model.prisma
//

model Batch {
  id       Int      @id @default(autoincrement())
  code     String   @unique
  expireAt DateTime

  medicationId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medication Medication      @relation(fields: [medicationId], references: [id])
  arrivals   ArrivalItem[]
  departures DepartureItem[]

  creatorUser    User  @relation(name: "BatchCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "BatchLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@unique([medicationId, code])
}

//
// inventory.model.prisma
//

model Inventory {
  id    Int     @id @default(autoincrement())
  stock Int     @default(0)
  price Decimal @default(0)
  min   Int     @default(0)

  pharmacyId   Int
  medicationId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  pharmacy   Pharmacy   @relation(fields: [pharmacyId], references: [id])
  medication Medication @relation(fields: [medicationId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser    User  @relation(name: "InventoryCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InventoryLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@unique([pharmacyId, medicationId])
}

model Arrival {
  id                       Int      @id @default(autoincrement())
  remark                   String
  arrivalDate              DateTime
  invoiceNumber            Int?
  invoiceAuthorizationCode String?
  invoiceControlCode       String?
  invoiceTotalRefPrice     Decimal  @default(0)

  approvalState    Int       @default(0)
  approvalDate     DateTime?
  approvalUserName String?

  closed Boolean @default(false)

  pharmacyId Int
  providerId Int?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  pharmacy Pharmacy      @relation(fields: [pharmacyId], references: [id])
  provider Provider?     @relation(fields: [providerId], references: [id])
  items    ArrivalItem[]

  creatorUser    User  @relation(name: "ArrivalCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ArrivalLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model ArrivalItem {
  id       Int     @id @default(autoincrement())
  quantity Int
  price    Decimal

  arrivalId Int
  batchId   Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  arrival Arrival @relation(fields: [arrivalId], references: [id])
  batch   Batch   @relation(fields: [batchId], references: [id])

  creatorUser    User  @relation(name: "ArrivalItemCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ArrivalItemLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model Departure {
  id            Int      @id @default(autoincrement())
  remark        String
  departureDate DateTime

  pharmacyId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  pharmacy Pharmacy        @relation(fields: [pharmacyId], references: [id])
  items    DepartureItem[]

  creatorUser    User  @relation(name: "DepartureCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "DepartureLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model DepartureItem {
  id       Int     @id @default(autoincrement())
  quantity Int
  price    Decimal

  departureId Int
  batchId     Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  departure Departure @relation(fields: [departureId], references: [id])
  batch     Batch     @relation(fields: [batchId], references: [id])

  creatorUser    User  @relation(name: "DepartureItemCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "DepartureItemLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// medication.model.prisma
//

model Medication {
  id            Int     @id @default(autoincrement())
  code          String  @unique
  name          String
  concentration String
  liname        Boolean

  classId Int
  unitId  Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  class     DrugClass   @relation(fields: [classId], references: [id])
  unit      DrugUnit    @relation(fields: [unitId], references: [id])
  inventory Inventory[]
  batches   Batch[]

  creatorUser                  User                           @relation(name: "MedicationCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser               User?                          @relation(name: "MedicationLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  PrescriptionMedication       PrescriptionMedication[]
  PrescriptionExternMedication PrescriptionExternMedication[]
}

//
// pharmacy.model.prisma
//

model Pharmacy {
  id   Int    @id @default(autoincrement())
  name String

  belongingId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  belonging  Belonging   @relation(fields: [belongingId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  inventory  Inventory[]
  arrivals   Arrival[]
  departures Departure[]

  creatorUser          User                   @relation(name: "PharmacyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser       User?                  @relation(name: "PharmacyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  PrescriptionPharmacy PrescriptionPharmacy[]
}

//
// clerk.model.prisma
//

model Clerk {
  id  Int @id @default(autoincrement())
  ein Int @unique //Employer Identification Number

  personId       Int @unique
  employeeTypeId Int
  positionId     Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  person       Person           @relation(fields: [personId], references: [id])
  employeeType EmployeeType     @relation(fields: [employeeTypeId], references: [id])
  position     EmployeePosition @relation(fields: [positionId], references: [id])
  clerkUser    ClerkUser?

  medicalOffices ClerkMedicalOffice[]

  creatorUser    User  @relation(name: "ClerkCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClerkLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model ClerkUser {
  id Int @id @default(autoincrement())

  userId  Int @unique
  clerkId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  user  User  @relation(fields: [userId], references: [id])
  clerk Clerk @relation(fields: [clerkId], references: [id])

  creatorUser    User  @relation(name: "ClerkUserCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClerkUserLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model ClerkMedicalOffice {
  id Int @unique @default(autoincrement())

  medicalOfficeId Int
  clerkId         Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalOffice MedicalOffice @relation(fields: [medicalOfficeId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  clerk         Clerk         @relation(fields: [clerkId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser    User  @relation(name: "ClerkMedicalOfficeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClerkMedicalOfficeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalOfficeId, clerkId])
}

//
// insured.model.prisma
//

model Insured {
  id         Int       @id @default(autoincrement())
  code       String    @unique
  iin        Int? //Insured Identification Number
  inletDate  DateTime
  outletDate DateTime?
  tradeUnion Boolean
  address    String?
  phone      String?

  holderInsuredId Int?
  personId        Int  @unique
  insuredTypeId   Int
  belongingId     Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  holderInsured Insured?    @relation(name: "HolderInsured", fields: [holderInsuredId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  dependents    Insured[]   @relation(name: "HolderInsured")
  person        Person      @relation(fields: [personId], references: [id])
  insuredType   InsuredType @relation(fields: [insuredTypeId], references: [id])
  belonging     Belonging   @relation(fields: [belongingId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser       User                @relation(name: "InsuredCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser    User?               @relation(name: "InsuredLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  ClinicCareInsured ClinicCareInsured[]
}

//
// person.model.prisma
//

model Person {
  id        Int      @id @default(autoincrement())
  firstName String
  lastName  String
  sex       String
  birthDate DateTime

  documentNumber       String?
  personDocumentTypeId Int?

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  personDocumentType PersonDocumentType? @relation(fields: [personDocumentTypeId], references: [id])

  clerk   Clerk?
  insured Insured?

  creatorUser    User  @relation(name: "PersonCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "PersonLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// clinic-care-primary.model.prisma
//

model ClinicCarePrimary {
  id           Int     @id @default(autoincrement())
  reason       String?
  physicalExam String?
  diagnosis    String?

  clinicCareId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  clinicCare ClinicCare @relation(fields: [clinicCareId], references: [id])

  creatorUser    User  @relation(name: "ClinicCarePrimaryCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClinicCarePrimaryLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// clinic-care.model.prisma
//

model ClinicCare {
  id        Int       @id @default(autoincrement())
  startDate DateTime
  endDate   DateTime?

  stateId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  primary       ClinicCarePrimary?
  insured       ClinicCareInsured?
  medicalOffice ClinicCareMedicalOffice?

  state ClinicalCareState @relation(fields: [stateId], references: [id])

  interclinicals      Interclinical[]
  prescriptions       Prescription[]
  prescriptionExterns PrescriptionExtern[]
  medicalLeaves       MedicalLeave[]

  creatorUser    User  @relation(name: "ClinicCareCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClinicCareLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model ClinicCareInsured {
  id              Int    @id @default(autoincrement())
  insuredIin      Int?
  personFirstName String
  personLastName  String
  insuredTypeName String

  insuredId    Int
  clinicCareId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  insured    Insured    @relation(fields: [insuredId], references: [id])
  clinicCare ClinicCare @relation(fields: [clinicCareId], references: [id])

  creatorUser    User  @relation(name: "ClinicCareInsuredCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClinicCareInsuredLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model ClinicCareMedicalOffice {
  id                Int    @id @default(autoincrement())
  medicalOfficeName String

  medicalOfficeId Int
  clinicCareId    Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalOffice MedicalOffice @relation(fields: [medicalOfficeId], references: [id])
  clinicCare    ClinicCare    @relation(fields: [clinicCareId], references: [id])

  creatorUser    User  @relation(name: "ClinicCareMedicalOfficeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ClinicCareMedicalOfficeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// inter-clinical.model.prisma
//

model Interclinical {
  id            Int      @id @default(autoincrement())
  remark        String
  driftDate     DateTime
  approvedState Int      @default(0)

  clinicCareId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  clinicCare ClinicCare @relation(fields: [clinicCareId], references: [id])

  provider     InterclinicalProvider?
  medicalGroup InterclinicalMedicalGroup?
  files        InterclinicalFileUpload[]

  creatorUser    User  @relation(name: "InterclinicalCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InterclinicalLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model InterclinicalProvider {
  id                   Int    @id @default(autoincrement())
  providerBusinessName String

  providerId      Int
  interclinicalId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  provider      Provider      @relation(fields: [providerId], references: [id])
  interclinical Interclinical @relation(fields: [interclinicalId], references: [id])

  creatorUser    User  @relation(name: "InterclinicalProviderCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InterclinicalProviderLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model InterclinicalMedicalGroup {
  id               Int    @id @default(autoincrement())
  medicalGroupName String

  medicalGroupId  Int
  interclinicalId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalGroup  MedicalGroup  @relation(fields: [medicalGroupId], references: [id])
  interclinical Interclinical @relation(fields: [interclinicalId], references: [id])

  specialties InterclinicalMedicalSpecialty[]

  creatorUser    User  @relation(name: "InterclinicalMedicalGroupCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InterclinicalMedicalGroupLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model InterclinicalMedicalSpecialty {
  id            Int    @unique @default(autoincrement())
  specialtyName String

  medicalSpecialtyId          Int
  interclinicalMedicalGroupId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalSpecialty          MedicalSpecialty          @relation(fields: [medicalSpecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  interclinicalMedicalGroup InterclinicalMedicalGroup @relation(fields: [interclinicalMedicalGroupId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  subspecialties InterclinicalMedicalSubspecialty[]

  creatorUser    User  @relation(name: "InterclinicalMedicalSpecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InterclinicalMedicalSpecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalSpecialtyId, interclinicalMedicalGroupId])
}

model InterclinicalMedicalSubspecialty {
  id               Int    @unique @default(autoincrement())
  subspecialtyName String

  medicalSubspecialtyId           Int
  interclinicalMedicalSpecialtyId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalSubspecialty           MedicalSubspecialty           @relation(fields: [medicalSubspecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  interclinicalMedicalSpecialty InterclinicalMedicalSpecialty @relation(fields: [interclinicalMedicalSpecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser    User  @relation(name: "InterclinicalMedicalSubspecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InterclinicalMedicalSubspecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalSubspecialtyId, interclinicalMedicalSpecialtyId])
}

model InterclinicalFileUpload {
  id Int @unique @default(autoincrement())

  fileUploadId    Int
  interclinicalId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  file          FileUpload    @relation(fields: [fileUploadId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  interclinical Interclinical @relation(fields: [interclinicalId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser    User  @relation(name: "InterclinicalFileUploadCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "InterclinicalFileUploadLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([interclinicalId, fileUploadId])
}

//
// medical-leave.model.prisma
//

model MedicalLeave {
  id        Int      @id @default(autoincrement())
  reason    String
  startDate DateTime
  endDate   DateTime

  approvalState    Int       @default(0)
  approvalDate     DateTime?
  approvalUserName String?

  clinicCareId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  approvalUser User?      @relation(name: "MedicalLeaveApprovalUser", fields: [approvalUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  clinicCare   ClinicCare @relation(fields: [clinicCareId], references: [id])

  disabilityType MedicalLeaveDisabilityType?

  creatorUser    User  @relation(name: "MedicalLeaveCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "MedicalLeaveLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model MedicalLeaveDisabilityType {
  id                 Int    @id @default(autoincrement())
  disabilityTypeName String

  disabilityTypeId Int
  medicalLeaveId   Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  disabilityType DisabilityType @relation(fields: [disabilityTypeId], references: [id])
  medicalLeave   MedicalLeave   @relation(fields: [medicalLeaveId], references: [id])

  creatorUser    User  @relation(name: "MedicalLeaveDisabilityTypeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "MedicalLeaveDisabilityTypeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// prescription.model.prisma
//

model Prescription {
  id          Int    @id @default(autoincrement())
  quantity    Int
  indications String

  clinicCareId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  clinicCare ClinicCare @relation(fields: [clinicCareId], references: [id])

  pharmacy   PrescriptionPharmacy?
  medication PrescriptionMedication?

  creatorUser    User  @relation(name: "PrescriptionCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "PrescriptionLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model PrescriptionPharmacy {
  id           Int    @id @default(autoincrement())
  pharmacyName String

  pharmacyId     Int
  prescriptionId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  pharmacy     Pharmacy     @relation(fields: [pharmacyId], references: [id])
  prescription Prescription @relation(fields: [prescriptionId], references: [id])

  creatorUser    User  @relation(name: "PrescriptionPharmacyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "PrescriptionPharmacyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model PrescriptionMedication {
  id                      Int    @id @default(autoincrement())
  medicationCode          String
  medicationName          String
  medicationConcentration String
  medicationClass         String
  medicationUnit          String

  medicationId   Int
  prescriptionId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medication   Medication   @relation(fields: [medicationId], references: [id])
  prescription Prescription @relation(fields: [prescriptionId], references: [id])

  creatorUser    User  @relation(name: "PrescriptionMedicationCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "PrescriptionMedicationLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model PrescriptionExtern {
  id          Int    @id @default(autoincrement())
  quantity    Int
  indications String

  clinicCareId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  clinicCare ClinicCare @relation(fields: [clinicCareId], references: [id])

  medication PrescriptionExternMedication?

  creatorUser    User  @relation(name: "PrescriptionExternCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "PrescriptionExternLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model PrescriptionExternMedication {
  id                      Int    @id @default(autoincrement())
  medicationCode          String
  medicationName          String
  medicationConcentration String
  medicationClass         String
  medicationUnit          String

  medicationId         Int
  prescriptionExternId Int @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medication         Medication         @relation(fields: [medicationId], references: [id])
  prescriptionExtern PrescriptionExtern @relation(fields: [prescriptionExternId], references: [id])

  creatorUser    User  @relation(name: "PrescriptionExternMedicationCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "PrescriptionExternMedicationLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// belonging.model.prisma
//

model Belonging {
  id   Int    @id @default(autoincrement())
  name String @unique

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalOffices MedicalOffice[]
  insureds       Insured[]
  providers      Provider[]
  pharmacies     Pharmacy[]

  creatorUser    User  @relation(name: "BelongingCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "BelongingLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

//
// medical-office.model.prisma
//

model MedicalOffice {
  id   Int    @id @default(autoincrement())
  name String @unique

  belongingId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  belonging Belonging            @relation(fields: [belongingId], references: [id])
  clerks    ClerkMedicalOffice[]

  creatorUser             User                      @relation(name: "MedicalOfficeCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser          User?                     @relation(name: "MedicalOfficeLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  ClinicCareMedicalOffice ClinicCareMedicalOffice[]
}

//
// provider.model.prisma
//

model Provider {
  id           Int    @id @default(autoincrement())
  vendorCode   String @unique
  businessName String

  nit     String?
  address String?
  phone   String?

  belongingId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  belonging      Belonging               @relation(fields: [belongingId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  medicalGroups  ProviderMedicalGroup[]
  interclinicals InterclinicalProvider[]
  arrivals       Arrival[]

  creatorUser    User  @relation(name: "ProviderCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ProviderLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}

model ProviderMedicalGroup {
  id Int @unique @default(autoincrement())

  medicalGroupId Int
  providerId     Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalGroup MedicalGroup @relation(fields: [medicalGroupId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  provider     Provider     @relation(fields: [providerId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  specialties ProviderMedicalSpecialty[]

  creatorUser    User  @relation(name: "ProviderMedicalGroupCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ProviderMedicalGroupLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalGroupId, providerId])
}

model ProviderMedicalSpecialty {
  id Int @unique @default(autoincrement())

  medicalSpecialtyId     Int
  providerMedicalGroupId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalSpecialty     MedicalSpecialty     @relation(fields: [medicalSpecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  providerMedicalGroup ProviderMedicalGroup @relation(fields: [providerMedicalGroupId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  subspecialties ProviderMedicalSubspecialty[]

  creatorUser    User  @relation(name: "ProviderMedicalSpecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ProviderMedicalSpecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalSpecialtyId, providerMedicalGroupId])
}

model ProviderMedicalSubspecialty {
  id Int @unique @default(autoincrement())

  medicalSubspecialtyId      Int
  providerMedicalSpecialtyId Int

  status Int @default(1)

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  medicalSubspecialty      MedicalSubspecialty      @relation(fields: [medicalSubspecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  providerMedicalSpecialty ProviderMedicalSpecialty @relation(fields: [providerMedicalSpecialtyId], references: [id], onDelete: NoAction, onUpdate: NoAction)

  creatorUser    User  @relation(name: "ProviderMedicalSubspecialtyCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "ProviderMedicalSubspecialtyLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)

  @@id([medicalSubspecialtyId, providerMedicalSpecialtyId])
}

//
// file.model.prisma
//

model FileUpload {
  id  Int    @id @default(autoincrement())
  md5 String @unique

  name      String
  encoding  String
  type      String
  extension String

  creatorUserName    String
  creationDate       DateTime
  lastUpdateUserName String?
  lastUpdateDate     DateTime?

  interclinicals InterclinicalFileUpload[]

  creatorUser    User  @relation(name: "FileUploadCreatorUser", fields: [creatorUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
  lastUpdateUser User? @relation(name: "FileUploadLastUpdateUser", fields: [lastUpdateUserName], references: [userName], onDelete: NoAction, onUpdate: NoAction)
}
